import { Typography } from '@mui/material';

const AdminDefault = () => {
    return (
        <Typography variant="h3" align="center" sx={{ color: 'white', my: 3 }}>
            Default Dashboard
        </Typography>
    );
};

export default AdminDefault;
