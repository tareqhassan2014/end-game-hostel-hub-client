import pxToRem from '../function/pxToRem';

const breadcrumbs = {
    styleOverrides: {
        li: {
            lineHeight: 0,
        },

        separator: {
            fontSize: pxToRem(14),
        },
    },
};

export default breadcrumbs;
