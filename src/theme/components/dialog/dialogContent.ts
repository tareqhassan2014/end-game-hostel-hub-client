import pxToRem from '../../function/pxToRem';

const dialogContent = {
    styleOverrides: {
        root: {
            padding: pxToRem(16),
            fontSize: pxToRem(16),
        },
    },
};

export default dialogContent;
