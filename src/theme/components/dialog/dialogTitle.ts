import pxToRem from '../../function/pxToRem';

const dialogTitle = {
    styleOverrides: {
        root: {
            padding: pxToRem(16),
            fontSize: pxToRem(20),
        },
    },
};

export default dialogTitle;
