import pxToRem from '../../function/pxToRem';

const dialogContentText = {
    styleOverrides: {
        root: {
            fontSize: pxToRem(16),
        },
    },
};

export default dialogContentText;
