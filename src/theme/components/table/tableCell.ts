import pxToRem from '../../function/pxToRem';

const tableCell = {
    styleOverrides: {
        root: {
            padding: `${pxToRem(12)} ${pxToRem(16)}`,
        },
    },
};

export default tableCell;
