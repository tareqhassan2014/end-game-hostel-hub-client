import pxToRem from '../function/pxToRem';

const popover = {
    styleOverrides: {
        paper: {
            padding: pxToRem(8),
            borderRadius: pxToRem(6),
        },
    },
};

export default popover;
